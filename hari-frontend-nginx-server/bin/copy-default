#!/bin/sh
mkdir -p /repo/config;
mkdir -p /repo/code;
if [ ! -f "/repo/config/letsencript.server.conf" ]
then
   envsubst '$DOMAIN' < /default/letsencript.server.conf > /repo/config/nginx.server.conf;
fi

